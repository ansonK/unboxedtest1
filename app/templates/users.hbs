<div class='row'>
  <div class='small-4 columns users-list'>
    {{input-eventable placeholder='Enter github username ...' value=newUsername onKey='createUser'}}
    {{#if errorMessage}}
      <div class='error'>{{errorMessage}}</div>
    {{/if}}
    <ul>
      {{#each}}
        <li>{{#link-to 'user' userName }}{{userName}}{{/link-to}}</li>
      {{/each}}
    </ul>
  </div>
  <div class='small-6 columns end'>
    {{outlet}}
  </div>
<div>
